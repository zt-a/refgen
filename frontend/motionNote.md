---

# 🎬 **Основные props для анимации в `motion`**

## ✅ **1. initial**

Начальное состояние анимации.

```tsx
initial={{ opacity: 0, y: 20 }}
```

---

## ✅ **2. animate**

Конечное состояние, куда элемент анимируется.

```tsx
animate={{ opacity: 1, y: 0 }}
```

---

## ✅ **3. exit** (только с AnimatePresence)

Как элемент уходит со страницы.

```tsx
exit={{ opacity: 0, x: -40 }}
```

---

# 🎚 **Настройки анимаций (transition)**

## ✅ **4. transition**

Определяет скорость, тип плавности и задержку.

```tsx
transition={{
  duration: 0.5,
  delay: 0.2,
  type: "spring",     // spring | tween | inertia
  stiffness: 200,
  damping: 20,
  ease: "easeInOut"   // ease, linear, easeIn, easeOut, easeInOut
}}
```

**Топ параметры transition:**

* `duration` — длительность
* `delay` — задержка
* `type` — тип анимации
* `ease` — плавность
* `stiffness` — жёсткость пружины
* `damping` — затухание пружины

---

# 🧩 **Props для взаимодействий**

## ✅ **5. whileHover**

Анимация при наведении мыши.

```tsx
whileHover={{ scale: 1.1 }}
```

---

## ✅ **6. whileTap**

При нажатии (mousedown/touchstart).

```tsx
whileTap={{ scale: 0.95 }}
```

---

## ✅ **7. whileFocus**

Когда элемент в фокусе клавиатурой.

---

## ✅ **8. whileInView**

Анимация запускается, когда элемент попадает в область видимости.

```tsx
whileInView={{ opacity: 1, y: 0 }}
viewport={{ once: true }}
```

---

# 🌐 **Viewport настройки**

## ✅ **9. viewport**

Как отслеживается попадание в видимость:

```tsx
viewport={{
  once: true,
  margin: "-100px"
}}
```

---

# 🎭 **Variants (для сложных анимаций)**

## ✅ **10. variants**

Наборы состояний, чтобы переиспользовать анимации.

```tsx
const v = {
  hidden: { opacity: 0 },
  visible: { opacity: 1 }
};
```

---

## ✅ **11. initial="hidden" / animate="visible"**

Когда используешь variants:

```tsx
<motion.div variants={v} initial="hidden" animate="visible" />
```

---

# 🌀 **Жесты и перетягивание**

## ✅ **12. drag**

Включает перетягивание:

```tsx
drag="x"      // или "y" или true
```

---

## ✅ **13. dragConstraints**

Ограничивает область перетягивания:

```tsx
dragConstraints={{ left: -100, right: 100 }}
```

---

## ✅ **14. dragElastic**

Насколько "резиновым" feels drag.

---

# 🎞 Другие полезные props

## 🔹 **15. style**

Можно анимировать через CSS переменные.

---

## 🔹 **16. layout**

Плавная анимация позиционирования:

```tsx
<motion.div layout />
```

---

## 🔹 **17. layoutId**

Плавное перемещение между страницами.

```tsx
<motion.div layoutId="card-1" />
```

---

# 📌 Краткая шпаргалка

| Prop              | Назначение              |
| ----------------- | ----------------------- |
| `initial`         | Начальное состояние     |
| `animate`         | Конечное состояние      |
| `exit`            | Анимация выхода         |
| `transition`      | Скорость, тип, задержка |
| `whileHover`      | При наведении           |
| `whileTap`        | При клике               |
| `whileFocus`      | При фокусе              |
| `whileInView`     | При появлении в окне    |
| `viewport`        | Настройки видимости     |
| `variants`        | Группы анимаций         |
| `drag`            | Перетягивание           |
| `dragConstraints` | Ограничения             |
| `layout`          | Плавная перестановка    |
| `layoutId`        | Анимация между экранами |

---
