# Base URL to your backend API (must already include /api prefix from backend routes).
# Examples:
#   VITE_API_URL=http://127.0.0.1:8000/api
#   VITE_API_URL=https://refgen.kg/api
#   VITE_API_URL=/api          # when nginx проксирует на backend в том же домене
VITE_API_URL=http://127.0.0.1:8000/api
